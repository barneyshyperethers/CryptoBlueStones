# Ethereum Network Configuration
# Replace with your actual values

# RPC URL for your Ethereum network (e.g., Infura, Alchemy, or local node)
RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID

# Private key of the account that will deploy contracts and interact with them
# WARNING: Never commit real private keys to version control!
PRIVATE_KEY=your_private_key_here

# Gas price in wei (optional, will use network default if not set)
GAS_PRICE=20000000000

# Gas limit for transactions (optional)
GAS_LIMIT=1000000
